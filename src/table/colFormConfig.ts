export const createColFormConfig = () => {
  let fConfig = {
    itemSpan: 12,
    items: [
      {
        field: 'title',
        title: '标题', //
        type: 'string',
      },
    ],
  }
}

export const createColTableConfig=()=>{
    
}